# Documentation moved

Project documentation now lives at the repository root:

**[`docs/`](../../docs/)**

`.hatchery/` remains project meta (branding, tooling, examples, audits) — not required to run Hatchery.
